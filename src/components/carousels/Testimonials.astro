---
import TestimonialsReact from './Testimonials';

// Use Astro's built-in i18n system
const rtl = Astro.currentLocale === 'ar';
---

<div class="opacity-100" style="opacity: 1;">
  <TestimonialsReact currentLocale={Astro.currentLocale as 'en' | 'ar'} client:only="react" />
</div>
